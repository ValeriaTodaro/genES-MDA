#Mod.py is the module that links GenES-MDA with the forward model
#It must be adapted according to each specific application

#Import here the required Python packages and libraries

'''
def write_input(par):
    #Function that writes the parameters into their proper location in the input files of the forward model.
    
def run():
    #Function that run the forward model in batch mode.

      
def read_output():
    #Function that reads the outputs of the forward model corresponding to the observation data.
    return values
'''